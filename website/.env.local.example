# Copy to website/.env.local — never commit .env.local
# Vercel: Project → Settings → Environment Variables

# Public (browser can read)
NEXT_PUBLIC_API_BASE_URL=https://your-api.onrender.com

# Server-only in Next.js (do NOT prefix with NEXT_PUBLIC_)
ADMIN_EMAIL=
ADMIN_PASSWORD=
